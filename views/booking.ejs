<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("head") %>
    <title>Workshop(Booking)</title>
</head>
<body>

    <%- include("header") %>

    <main>
        <div class="main">
            <div class="container">
                <form method="post" action="/booking">
                    <div class="heading">
                        
                    <h1>Book Now!</h1>
                    </div>

                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" placeholder="John Doe" required
                        onkeypress="return (event.charCode > 64 && event.charCode < 91) || (event.charCode > 96 && event.charCode < 123) || event.key == ' '"/>
                    <br>

                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="John123@gmail.com" required/>
                    <br>

                    <label for="phoneNumber">Phone number</label>
                    <input type="tel" minlength="12" maxlength="16" id="phoneNumber" name="phoneNumber" placeholder="353 000 000 0000" required
                        onkeypress="return event.charCode >= 48 && event.charCode <= 57"/>
                    <br>

                    <div class="Date">
                        <div class="startDate">
                            <label for="startDate">First day</label>
                            <input type="date" name="startDate" id="startDate" type="date" required placeholder="00/00/0000" required/>
                            <br>
                        </div>

                        <div class="endDate">
                            <label for="endDate">Last day</label>
                            <input type="date" id="endDate" name="endDate" type="date" required placeholder="00/00/0000" required/>
                            <br>
                        </div>
                    </div>

                    <label for="cardNumber">Card number</label>
                        <input type="text" minlength="16" maxlength="16" id="card" name="card" placeholder="0000 0000 0000 0000" required
                        onkeypress="return event.charCode >= 48 && event.charCode <= 57"/>

                    <label for="expiry">Expiration date</label>
                        <input class="inputCard" maxlength="5" name="expiry" id="expiry" type="text" required placeholder="00/00" required
                        onkeypress="return event.charCode >= 48 && event.charCode <= 57"/>

                    <label for="cvc">CVC</label>
                        <input type="text" minlength="3" maxlength="4" id="cvc" name="cvc" placeholder="XXX" required
                        onkeypress="return event.charCode >= 48 && event.charCode <= 57"/>

                    <input type="submit" value="Book Now">
                </form>
            </div>
        </div>
    </main>

    <%- include("footer") %>

</body>
</html>
